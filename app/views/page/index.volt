<section class="index">
	<div class="warp">
		<div class="index__logo" data-text="{{ config.site.title }}">{{ config.site.title }}</div>
		<div class="index__info">{{ slogan }}</div>
	</div>
</section>